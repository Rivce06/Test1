apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "mern-app.fullname" . }}-config
  labels:
    {{- include "mern-app.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.configMap.data | nindent 2 }}
